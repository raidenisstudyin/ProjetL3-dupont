extends layout

block content
  h1 #{title}: #{book.title}
  p= book.title 

    p Do you really want to delete this book ?

    form(method='POST' action='')
      div.form-group
        input#boookid.form-control(type='hidden',name='bookid', required='true', value=book._id  )

      button.btn.btn-primary(type='submit') Delete
